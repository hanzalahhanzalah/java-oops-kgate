import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        /*Write a Java program that takes an integer input from the user and determines whether it is a prime number or not.
        Use a loop to check for divisibility.

                Instructions:
        Use the Scanner class to take user input for an integer.
        Implement a loop to check for divisibility from 2 to the square root of the input number.
        If the number is divisible by any of these values, it is not a prime number.

                Print a message indicating whether the entered number is prime or not.
                Scanner sc = new Scanner(System.in);
        System.out.println("enter number: ");
        int number= 0;

        int ans;
        ans = number / 2;
        number = sc.nextInt();
        while (number > 0) {
            if (ans == 0) {
                System.out.println("prime no"+number+ ans);
                break;
            } else {
                System.out.println("not prime number");
            }
        }*/
        System.out.println(4/1);
        System.out.println(4/4);
        System.out.println(4%1);
        System.out.println(2/2);
        System.out.println(2/1 );
        System.out.println(2%1);
    }
}